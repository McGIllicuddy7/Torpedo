[package]
name = "torpedo"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.10.1"
raylib = {version = "5.5.1", features = ["serde_json", "with_serde"]}
rayon = "1.12.0"
serde = {version = "1.0.228", features = ["derive", "rc"]}
